import React from "react";
import Header = require("header");
import Main = require("main");
import Data-block = require("data-block");
import Footer = require("footer");

function include(props) {
	return (
		<>
			
		</>
	);
}

export default include;